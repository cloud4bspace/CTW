package space.cloud4b.ctw.model

/**
 * Die Klasse beinhaltet eine Liste von Objekten des Typs TeamMember
 * und wird für den TeamlistAdapter genutzt, um entsprechende JSON-Einträge für die
 * ListView aufzubereiten.
 * @author Serge Kaulitz & Bernhard Kämpf
 */
class Team(var teamlist: MutableList<TeamMember>) {
}