package space.cloud4b.ctw.model

/**
 * Die Klasse beinhaltet eine Liste von Objekten des Typs CakeboardEntry
 * und wird für den CakeboardAdapter genutzt, um entsprechende JSON-Einträge für die
 * ListView aufzubereiten.
 * @author Serge Kaulitz & Bernhard Kämpf
 */
class Cakeboard(val cakeboard : MutableList<CakeboardEntry>) {
}