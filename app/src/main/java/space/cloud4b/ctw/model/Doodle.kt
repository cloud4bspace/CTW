package space.cloud4b.ctw.model

/**
 * Die Klasse beinhaltet eine Liste von Objekten des Typs DoodleResponse
 * und wird für den DoodleAdapter genutzt, um entsprechende JSON-Einträge für die
 * ListView aufzubereiten.
 * @author Serge Kaulitz & Bernhard Kämpf
 */
class Doodle(val doodle : MutableList<DoodleResponse>) {

}